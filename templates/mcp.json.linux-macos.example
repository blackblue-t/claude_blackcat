{
  "_readme": "Copy to your project root as .mcp.json, delete servers you do not use, fill in keys. .mcp.json is machine-specific -- do NOT commit it.",
  "_pencil_note": "Frontend canvas-first flow: npm install -g @pencil.dev/cli (Node 18+), then 'pen login'. The desktop app wires its MCP automatically; for CLI-based MCP wiring copy the exact entry from https://docs.pencil.dev/for-developers/pen-cli into this file.",
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": { "GITHUB_PERSONAL_ACCESS_TOKEN": "ghp_YOUR_TOKEN_HERE" }
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"]
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    }
  }
}
